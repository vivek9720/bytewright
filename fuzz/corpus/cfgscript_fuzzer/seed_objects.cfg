# object literals with nested objects and arrays inside
endpoint = {
  host = "127.0.0.1";
  port = 8080;
  tags = ["primary", "tcp"];
  limits = {
    max_conns = 1024;
    timeout_ms = 2500;
    backoff = { initial = 100; factor = 2.0; };
  };
};
