# Build output
/build/
/out/
/cmake-build-*/
*.o
*.obj
*.a
*.lib
*.exe
*.pdb

# Fuzzing artifacts (keep fuzz/corpus/ and fuzz/dictionary.txt, ignore the rest)
/fuzz-out/
crash-*
leak-*
oom-*
timeout-*
*.profraw

# PoC working directory — the crashing input, notes, and the patch must NOT be
# committed to the submitted repo (the difficulty checker must not see the fix).
/poc/

# Editor / OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
